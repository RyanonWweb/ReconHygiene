[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# Redirect bare domain → www (or flip if you prefer apex)
# [[redirects]]
#   from   = "https://www.reconhygiene.com/*"
#   to     = "https://reconhygiene.com/:splat"
#   status = 301
#   force  = true
